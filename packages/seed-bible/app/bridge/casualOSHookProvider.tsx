import type { HookProvider } from "app.contract.provider";

/**
 * A hooks standard mapped to casualOS api's providing said functionality.
 */
const casualOSHookProvider: HookProvider = os.appHooks;

export default casualOSHookProvider;
