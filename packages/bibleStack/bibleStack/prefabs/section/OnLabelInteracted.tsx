/**
    * Triggers an OnSectionInteracted event when the section's label has been interacted.
    * @example
    * section.OnLabelInteracted()
*/

shout("OnSectionInteracted", {section: thisBot, typeOfInteraction: StackElementInteractionType.Tap});