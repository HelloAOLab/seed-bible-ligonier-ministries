import { MapToolProvider } from "interactiveBible.managers.MapsManager.MapToolContext"
import { MapWrapper } from "interactiveBible.managers.MapsManager.MapWrapperComponent"

export const MapToolModes = Object.freeze({
    Viewer: "Viewer",
    Checkbox: "Checkbox"
});

export const MapTool = ({mode = MapToolModes.Checkbox, onSelectionChange, arrangementIndex = 0}) => {
    return (
        <>
            <style>{thisBot.tags["MapPanel.css"]}</style>
            <MapToolProvider 
                mode={mode} 
                onSelectionChange={onSelectionChange ?? onSelectionChangeLocal} 
                arrangementIndex={arrangementIndex}
                selection={selection}
                MapToolModes={MapToolModes}
            >
                <MapWrapper />
            </ MapToolProvider>
        </>
    );
};