const keys = Object.keys({...thisBot.tags}).filter((key) => { 
    return (typeof thisBot.tags[key] === "string") && [...thisBot.tags[key]][0] == '🔗' 
}).filter((key) => {
    return getBot(byID([...thisBot.tags[key]].slice(1).join("")))
})

const linksMap = new Map(keys.map((key) => {
    const bot = getBot(byID([...thisBot.tags[key]].slice(1).join("")))
    return [key, bot.tags.system]
}))

console.log({linksMap})

return