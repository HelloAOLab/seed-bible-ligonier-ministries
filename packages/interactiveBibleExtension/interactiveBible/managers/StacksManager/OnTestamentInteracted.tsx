/**
    * Handles the interaction event with a testament element. Calls the method to process a testament interaction.
    *
    * @param {Object} that - An object containing relevant data for the interaction, including the testament element.
    * @example
    * shout("OnTestamentInteracted", {testament: someBot, typeOfInteraction: StackElementInteractionType.Tap});
*/

return thisBot.HandleTestamentInteraction(that);