/**
    * Handles interaction with a section by delegating the action to the section interaction handler.
    *
    * @param {Object} that - The context object containing interaction details.
    * @example
    * shout("OnSectionInteracted", {section: someBot, typeOfInteraction: StackElementInteractionType.Click});
*/

return thisBot.HandleSectionInteraction(that);