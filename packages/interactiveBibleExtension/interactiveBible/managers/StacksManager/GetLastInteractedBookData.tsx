/**
    * Retrieves the last interacted BookData stored in the StacksManager vars.
    *
    * @returns {string} - The last interacted BookData.
    * @example
    * const arrangementName = StacksManager.GetLastInteractedBookData();
*/

return thisBot.vars.lastInteractedBookData;