/**
    * Handles the event when the Bible creation has been completed by setting the isBibleAnimating tag mask to false.
    *
    * @example
    * shout("OnBibleCreationComplete");
*/

setTagMask(thisBot, "isBibleAnimating", false);